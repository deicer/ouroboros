services:
  ouroboros:
    build: .
    restart: on-failure
    cpus: 1.0
    mem_limit: 2g
    env_file: .env
    ports:
      - "${OUROBOROS_TRACE_WEB_PORT:-8088}:${OUROBOROS_TRACE_WEB_PORT:-8088}"
    volumes:
      - ./data:/data
